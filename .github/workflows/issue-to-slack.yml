name: Notify Slack on Issue Created

on:
  issues:
    types: [opened]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Check Slack Webhook
        run: |
          if [ -z "${{ secrets.SLACK_WEBHOOK }}" ]; then
            echo "Error: SLACK_WEBHOOK is not set or empty"
            exit 1
          else
            echo "SLACK_WEBHOOK is set"
          fi

      - name: Prepare Payload
        run: |
          # Debug raw labels input
          echo "${{ toJson(github.event.issue.labels) }}" > labels.json 2>/dev/null || echo "[]" > labels.json
          echo "Raw Labels JSON Content:"
          cat labels.json
          # Extract only label names to avoid invalid JSON
          jq -r '[.[]?.name // empty] | join(" ")' labels.json > labels.txt 2>/dev/null || echo "Not Set" > labels.txt
          echo "Extracted Labels Content:"
          cat labels.txt
          jq -n \
            --arg text "🐞 New GitHub Issue Created" \
            --arg title "${{ github.event.issue.title || 'No Title' }}" \
            --arg assignee "${{ github.event.issue.assignee.login || 'Unassigned' }}" \
            --arg Labels "$(cat labels.txt)" \
            --arg issue_url "${{ github.event.issue.html_url || 'No URL' }}" \
            '{
              "text": $text,
              "blocks": [
                {
                  "type": "header",
                  "text": { "type": "plain_text", "text": $text, "emoji": true }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*📌 Project:*\nRevolt Motors Website" },
                    { "type": "mrkdwn", "text": "*🚨 Labels:*\n\($Labels)" },
                    { "type": "mrkdwn", "text": "*📝 Title:*\n\($title)" },
                    { "type": "mrkdwn", "text": "*👤 Assigned To:*\n\($assignee)" }
                  ]
                },
                {
                  "type": "section",
                  "text": { "type": "mrkdwn", "text": "<\($issue_url)|🔗 View Issue>" }
                }
              ]
            }' > payload.json
          echo "Payload JSON Content:"
          cat payload.json
          jq . payload.json 2>/dev/null || echo "Invalid JSON in payload.json"

      - name: Send Slack Notification via curl
        run: |
          curl -X POST -H 'Content-type: application/json' --data @payload.json ${{ secrets.SLACK_WEBHOOK }} || echo "curl failed"
        continue-on-error: true

      - name: Debug Curl Notification
        run: echo "Curl notification step completed with exit code $?"
